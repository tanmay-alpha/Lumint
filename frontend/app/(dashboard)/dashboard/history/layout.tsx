export const metadata = {
  title: "Scan History · Lumint",
  description: "Past UPI, document, and URL scans — stored locally on your device only.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
