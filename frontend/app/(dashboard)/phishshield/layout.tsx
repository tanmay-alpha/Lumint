export const metadata = {
  title: "PhishShield · Lumint",
  description: "Analyze suspicious URLs in real time for typosquatting, domain age, and phishing indicators.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
