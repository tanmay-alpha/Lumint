export const metadata = {
  title: "DocShield · Lumint",
  description: "Client-side document forensics: detect tampered PDFs, ID scans, and invoices with ELA and metadata checks.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
