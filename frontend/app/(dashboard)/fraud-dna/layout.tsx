export const metadata = {
  title: "Fraud DNA · Lumint",
  description: "Visualize fraud campaign networks and shared infrastructure between phishing attacks.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
