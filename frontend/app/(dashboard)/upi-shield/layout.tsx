export const metadata = {
  title: "UPI Shield · Lumint",
  description: "Detect fake UPI payment screenshots from PhonePe, Google Pay, Paytm, and BHIM using OCR and forensics.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
