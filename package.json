{
  "name": "lumint-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "dev": "node start-dev.js",
    "dev:frontend": "npm run dev --workspace=frontend"
  },
  "devDependencies": {
    "next": "^16.2.7"
  }
}
